# 第二阶段：任务 Kanban Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Square UI 的示范任务换成 Supabase 真实任务，让员工和管理员建立、分派、留言、筛选和拖动任务。

**Architecture:** 页面从服务器取得任务，所有新增和修改通过带 Clerk 检查的 Server Actions。拖动先在画面上即时反映，再写入 Supabase；失败时恢复原位并显示原因。

**Tech Stack:** Next.js Server Actions、Supabase Postgres、Zod、dnd-kit、Base UI、Vitest、Testing Library、Playwright

**Spec:** `docs/superpowers/specs/2026-08-28-internal-task-content-scheduling-design.md`

## Global Constraints

- 任务状态固定为 `todo`, `in_progress`, `review`, `done`。
- 优先级固定为 `low`, `medium`, `urgent`。
- 一般任务和以后自动建立的发布任务共用同一张表。
- 删除任务只写 `archived_at`，不永久删除。
- 每个 Server Action 先调用 `getVerifiedUser()`，不能信任浏览器传来的操作者编号。

---

### Task 1: 建立任务资料表和纯资料格式

**Files:**
- Create: `supabase/migrations/202608280002_tasks.sql`, `features/tasks/types.ts`, `features/tasks/schema.ts`, `features/tasks/status.ts`
- Test: `tests/features/tasks/schema.test.ts`, `tests/features/tasks/status.test.ts`

**Interfaces:**
- Produces: `TaskStatus`, `TaskPriority`, `TaskRecord`, `taskInputSchema`。
- Produces: `groupTasksByStatus(tasks): Record<TaskStatus, TaskRecord[]>`。

- [x] **Step 1: 写格式测试**

```ts
it("rejects an empty title", () => {
  expect(taskInputSchema.safeParse({ title: "", assigneeId: "u1", priority: "medium", dueAt: "2026-08-29T02:00:00.000Z" }).success).toBe(false);
});
```

- [x] **Step 2: 建立表**

```sql
create type task_status as enum ('todo', 'in_progress', 'review', 'done');
create type task_priority as enum ('low', 'medium', 'urgent');
create type task_kind as enum ('general', 'content_publish');

create table tasks (
  id uuid primary key default gen_random_uuid(),
  title text not null check (char_length(title) between 1 and 200),
  description text not null default '',
  status task_status not null default 'todo',
  priority task_priority not null default 'medium',
  kind task_kind not null default 'general',
  assignee_id text not null references profiles(clerk_user_id),
  creator_id text not null references profiles(clerk_user_id),
  due_at timestamptz not null,
  position numeric not null default 1000,
  linked_content_id uuid,
  archived_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table task_comments (
  id uuid primary key default gen_random_uuid(),
  task_id uuid not null references tasks(id),
  author_id text not null references profiles(clerk_user_id),
  body text not null check (char_length(body) between 1 and 5000),
  created_at timestamptz not null default now()
);
```

- [x] **Step 3: 实现纯函数并运行测试**

```ts
export const TASK_STATUSES = ["todo", "in_progress", "review", "done"] as const;
export type TaskStatus = typeof TASK_STATUSES[number];
```

```bash
pnpm exec supabase db reset
pnpm test tests/features/tasks
git add supabase features/tasks tests/features/tasks
git commit -m "feat: add task data model"
```

执行记录：本机没有 Docker，因此以 PGlite 实际执行 profiles 与 tasks 两份迁移，并验证四种任务状态、任务表、留言表和 RLS 均成功建立。

### Task 2: 建立任务后台操作

**Files:**
- Create: `features/tasks/repository.ts`, `features/tasks/actions.ts`, `features/tasks/queries.ts`
- Test: `tests/features/tasks/actions.test.ts`

**Interfaces:**
- Produces: `listTasks(filters)`, `createTask(input)`, `updateTask(id, input)`, `moveTask(id, status, position)`, `archiveTask(id)`, `addTaskComment(taskId, body)`。

- [x] **Step 1: 写未登录拒绝测试**

```ts
it("does not create a task without a verified user", async () => {
  mockedGetVerifiedUser.mockRejectedValue(new Error("UNAUTHENTICATED"));
  await expect(createTask(validTaskInput)).rejects.toThrow("UNAUTHENTICATED");
  expect(mockedInsert).not.toHaveBeenCalled();
});
```

- [x] **Step 2: 实现 repository**

所有查询都加 `.is("archived_at", null)`；`createTask` 的 `creator_id` 来自 `getVerifiedUser().id`；`moveTask` 同时写 `status` 和 `position`。

- [x] **Step 3: 实现 Server Actions**

每个动作依次执行：检查登录、Zod 检查输入、调用 repository、`revalidatePath("/tasks")`。动作返回固定格式：

```ts
type ActionResult<T> = { ok: true; data: T } | { ok: false; message: string };
```

- [x] **Step 4: 运行测试并提交**

```bash
pnpm test tests/features/tasks/actions.test.ts
git add features/tasks tests/features/tasks
git commit -m "feat: add authenticated task actions"
```

### Task 3: 把示范看板换成真实 Kanban

**Files:**
- Create: `app/(protected)/tasks/page.tsx`, `features/tasks/components/task-board.tsx`, `features/tasks/components/task-column.tsx`, `features/tasks/components/task-card.tsx`, `features/tasks/components/task-form.tsx`, `features/tasks/components/task-filters.tsx`
- Modify: `components/tasks/tasks-main.tsx`, `components/app-shell/app-sidebar.tsx`
- Delete: `mock-data/tasks.ts`, `mock-data/statuses.tsx`, `store/tasks-store.ts`
- Test: `tests/features/tasks/task-board.test.tsx`

**Interfaces:**
- Consumes: `TaskRecord[]`, `moveTask()`。
- Produces: `TaskBoard({ initialTasks })`。

- [ ] **Step 1: 安装拖动套件并写看板测试**

```bash
pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

```tsx
it("renders the four task columns", () => {
  render(<TaskBoard initialTasks={[]} />);
  for (const name of ["还没开始", "正在做", "等人检查", "已经完成"]) {
    expect(screen.getByText(name)).toBeInTheDocument();
  }
});
```

- [ ] **Step 2: 实现服务器页面和筛选**

`/tasks` 服务器页面读取 `search`, `priority`, `assignee` 查询参数，再调用 `listTasks()`。日期统一用 `Intl.DateTimeFormat("zh-MY", { timeZone: "Asia/Kuala_Lumpur" })`。

- [ ] **Step 3: 实现拖动和失败恢复**

`onDragEnd` 保存旧任务数组，先更新本地状态，再调用 `moveTask`；当结果是 `{ ok: false }` 时恢复旧数组并显示 toast。卡片键盘拖动使用 dnd-kit `KeyboardSensor`。

- [ ] **Step 4: 实现新增和修改弹窗**

表单包含标题、说明、负责人、完成日期时间和优先级。负责人只可从未收起的 `profiles` 选择。

- [ ] **Step 5: 运行组件测试并提交**

```bash
pnpm test tests/features/tasks/task-board.test.tsx
git add app components features package.json pnpm-lock.yaml
git commit -m "feat: replace mock board with task kanban"
```

### Task 4: 加入任务详情、留言和收起

**Files:**
- Create: `app/(protected)/tasks/[taskId]/page.tsx`, `features/tasks/components/task-comments.tsx`, `features/tasks/components/archive-task-button.tsx`
- Test: `tests/features/tasks/task-comments.test.tsx`, `tests/e2e/tasks.spec.ts`

**Interfaces:**
- Consumes: `addTaskComment()`, `archiveTask()`。

- [ ] **Step 1: 写空留言拒绝测试**

```tsx
it("does not submit whitespace-only comments", async () => {
  render(<TaskComments taskId="11111111-1111-1111-1111-111111111111" comments={[]} />);
  await userEvent.type(screen.getByRole("textbox"), "   ");
  expect(screen.getByRole("button", { name: "留言" })).toBeDisabled();
});
```

- [ ] **Step 2: 实现详情和留言**

留言显示头像、名字、马来西亚时间和内容；发送后清空输入框并刷新留言数量。

- [ ] **Step 3: 实现收起确认**

“收起任务”必须先显示确认弹窗。自动发布任务不能从这里收起，避免内容和任务失去连接。

- [ ] **Step 4: 完成第二阶段检查**

```bash
pnpm test
pnpm test:e2e
pnpm lint
pnpm build
git add .
git commit -m "feat: add task detail comments and archive"
```

人工检查：建立任务、换负责人、筛选、拖过四栏、刷新后位置不变、留言、收起、手机宽度横向滚动、键盘可以拖动。
